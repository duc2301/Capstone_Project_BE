namespace Domain.Enum.Site
{
    // Mức giải tỏa mặt bằng của lô đất / hộ
    public enum ClearanceStatus
    {
        NotCleared,
        Partial,    // giải tỏa 1 phần
        Full        // giải tỏa toàn phần
    }
}
