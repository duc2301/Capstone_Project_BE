namespace Domain.Enum.Site
{
    public enum SiteAnnotationType
    {
        Point,      // "số lượng" - pick điểm cần bay chụp
        Distance,   // "khoảng cách" - đo trên bản đồ
        Area,       // "diện tích"
        Note        // ghi chú thường
    }
}
