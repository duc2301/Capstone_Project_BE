namespace Domain.Enum.Submittal
{
    public enum SubmittalStepType
    {
        Submit,   // Trình nộp (nhà thầu)
        Verify,   // Thẩm tra (tư vấn giám sát)
        Approve   // Duyệt (chủ đầu tư)
    }
}
