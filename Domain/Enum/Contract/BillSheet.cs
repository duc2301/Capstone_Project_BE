namespace Domain.Enum.Contract
{
    // Sheet A = khối lượng trong hợp đồng; Sheet B = phát sinh ngoài hợp đồng (dấu X)
    public enum BillSheet
    {
        InContract,
        OutOfContract
    }
}
