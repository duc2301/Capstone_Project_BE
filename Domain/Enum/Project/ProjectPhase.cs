namespace Domain.Enum.Project
{
    // Vòng đời dự án (toàn vòng đời)
    public enum ProjectPhase
    {
        Concept,
        Design,
        Construction,
        Handover,
        Operation
    }
}
