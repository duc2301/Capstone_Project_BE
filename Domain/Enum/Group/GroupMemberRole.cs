namespace Domain.Enum.Group
{
    // Vai trò của 1 Account trong 1 Group (per-group, không phải hệ thống).
    public enum GroupMemberRole
    {
        Member,   // thành viên thường
        Leader    // trưởng nhóm
    }
}
